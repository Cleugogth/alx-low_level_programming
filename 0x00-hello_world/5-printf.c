#include <stdio.h>

/**
 * main - C program that prints using printf
 *
 * Return: Always 0 (Success)
 *
 */
int main(void)
{
	char *P = "with proper grammar, but the outcome is a piece of art,";
	printf("%s\n", P);

	return (0);
}

